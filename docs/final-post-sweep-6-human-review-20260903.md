# final post-sweep 6 条抽样人工审核

## 范围

- 抽样文件：`runtime/run133-delta11-v3-20260903-171758-all.review.jsonl`
- 抽样规模：`840` 条
- 标签数量：`140` 个
- 每个标签：`6` 条
- 文件校验：JSONL 可解析，所有记录均为 `final-label-post-sweep-review-packet-v1`，每个标签恰好 6 条。

审核标准是：题干/解析/答案是否直接支持 `verify_label`，并把标签错误、题目或答案损坏、以及包内元数据残留分开记录。该抽样是 spot-check，不替代每个标签正式的 60 条独立复核。

## 标签级结论

按文件中的 140 个标签逐个审核。绝大多数标签的 6 条样本都与目标知识点直接对应；没有发现需要整标签推翻的普遍性问题。以下 2 个标签出现明确的标签级疑点：

| 标签 | 样本 | 结论 |
|---|---:|---|
| `知识点@词汇@近/反义词@同/近义词` | 6 | 1 条明确错标（`3420852316601602048`）：`borrow` 的近义词题答案给成 `lend`，实际为反义方向；另 1 条（`2956984591658618880`）的选项 `thing` 与 `item` 同时成立，题目有歧义。 |
| `知识点@语法词法@动词时态@一般将来时@一般将来时的定义/判定@be going to` | 6 | 1 条作文样本（`2728639031827537923`）只使用 `will`，题干/答案没有 `be going to`，按末级标签严格定义不应进入该标签；若按参考范文标注，应改到 `will/shall do结构`。 |
| `知识点@语篇主题@人与社会@中外重要人物` | 6 | 6 条均为人物/人物生平或人物事迹，未发现标签级错标。 |

其余标签在这 6 条样本中均未发现明确的标签级错标；边界标签仍应以正式独立复核为准。

## 需要单独处理的题目/答案问题

这些问题不等于知识点标签错误，但不建议原样进入训练集：

| 标签 | question_id | 问题 |
|---|---|---|
| `知识点@词汇@构词法@缩写/简写法` | `2858711530726789120` | `SOS` 更像求救信号而非缩写；属于定义边界样本。 |
| `知识点@词汇@近/反义词@同/近义词` | `2956984591658618880` | `thing` 与 `item` 都可作 `object` 的近义词，选项有歧义。 |
| `知识点@语法句法@主从复合句@宾语从句@宾语从句的否定转移` | `2847112783436537856` | 选项 C 才与解析一致，但答案字段写成 A；答案键冲突。 |
| `知识点@语法句法@句子种类@选择疑问句` | `2139845912059547648` | 答案字段为 `ebK1`，明显损坏；题型标签本身仍可判断。 |
| `知识点@语法词法@介词@时间介词` | `2805367419965329408` | 只有“用 for 或 since 完成下列句子”，缺少实际题干/答案，无法验证；应隔离。 |
| `知识点@语法词法@动词@情态动词@had better` | `2759216265340416003` | 小题文本为 `you'd better out if...`，缺少 `check` 等内容；输入损坏。 |
| `知识点@语法词法@动词@情态动词@must@mustn't表示禁止` | `3122317387568787456` | 解析说答案为 C，但正确选项是 B；答案键冲突。 |
| `知识点@语法词法@数词@数词的特殊用法@hundred, thousand, million等词的用法` | `2139853658251169796` | `hundreds of yuan` 应选 D，答案字段写成 A；答案键冲突。 |
| `知识点@语法词法@数词@数词的特殊用法@倍数的表达` | `2141436042464276480`, `2547195260760522752` | 解析/答案漏写结构中的 `as`，应回查答案键。 |
| `知识点@语法词法@被动语态@含情态动词的被动语态` | `3019207414525804544` | `valued`（被动）和 `valuable`（形容词）均被接受，标签证据不唯一。 |
| `知识点@语法句法@主从复合句@宾语从句@宾语从句的时态@主过从必过` | `3284914139058700297` | 所考空格是主句 `realized` 的一般过去时，并未真正检验“主过从必过”；属于弱证据。 |
| `知识点@语法词法@非谓语动词@动词不定式@动词不定式的句法功能@不定式作宾语` | `2745440949406130176` | `seem to make sense` 中不定式更像系动词补语，不是典型宾语；属于弱证据。 |

## 包内容与多模态注意事项

- `96/840` 条仍含“根据以上信息，当前小题所属的题型方法类目和知识点类目为：”这一类题型元数据尾巴。它不改变上述标签判断，但正式训练前应清除。
- `1/840` 条含音频标记（`2735577777580818432`）。文本解析给出了答案，但没有音频文件时不能把音频相关判定当作完全独立的人工确认。
- 一些语篇主题/体裁样本没有“题目答案”字段，这是整篇阅读材料或作文/通知本身，不是自动判错；主题/体裁标签可以依据篇章内容审核。

## 处理建议

1. 先从训练候选中剔除上表的明确错误、答案键损坏和不完整输入；保留原始记录到 `relabel_candidates`。
2. 对三个标签级疑点做定向回查，尤其是 `同/近义词`、`be going to`、`中外重要人物`。
3. 清理 96 条题型元数据残留后再生成最终训练文件。
4. 本次 6 条抽样只能说明“没有发现大面积异常”，不能替代每标签 60 条独立复核或最终 released-silver gate。
